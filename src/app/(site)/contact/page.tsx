import { Mail, MapPin, Phone } from "lucide-react";
import { LeadForm } from "@/components/lead-form";
import { Container, Eyebrow, Section } from "@/components/ui";
import { siteConfig, pageMetadata } from "@/lib/site";

export const metadata = pageMetadata("Contact", "Start a conversation with Ventrio about technology consulting, product development, AI, cloud, and custom software.", "/contact");
export default function ContactPage() { const contactItems = [siteConfig.email !== "hello@ventrio.example" ? { icon: Mail, label: "Email", value: siteConfig.email, href: `mailto:${siteConfig.email}` } : null, siteConfig.phone ? { icon: Phone, label: "Phone", value: siteConfig.phone, href: `tel:${siteConfig.phone}` } : null].filter(Boolean) as { icon: typeof Mail; label: string; value: string; href: string }[]; return <><section className="border-b border-zinc-200 bg-zinc-50 py-16 sm:py-22"><Container><Eyebrow>Contact</Eyebrow><h1 className="mt-5 max-w-3xl text-4xl font-semibold tracking-tight text-zinc-950 sm:text-6xl">Let&apos;s build something great.</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-zinc-600">Tell us about the opportunity, the thorny workflow, or the system that needs a better future. We&apos;ll help find a practical way forward.</p></Container></section><Section><Container><div className="grid gap-12 lg:grid-cols-[0.8fr_1.2fr]"><div><h2 className="text-2xl font-semibold tracking-tight text-zinc-950">Start with a conversation.</h2><p className="mt-4 max-w-sm text-sm leading-6 text-zinc-600">We do not publish an office address until it is configured. For now, use the form and a Ventrio team member will respond directly.</p>{contactItems.length > 0 ? <div className="mt-8 space-y-4">{contactItems.map((item) => { const Icon = item.icon; return <a key={item.label} href={item.href} className="flex items-center gap-3 text-sm font-medium text-zinc-800 hover:text-blue-600"><span className="grid size-9 place-items-center rounded-md bg-blue-50 text-blue-700"><Icon className="size-4" /></span>{item.value}</a>; })}</div> : <div className="mt-8 rounded-md border border-dashed border-zinc-300 p-5 text-sm leading-6 text-zinc-500"><MapPin className="mb-3 size-4" />Direct email, phone, social links, and a location map appear here once Ventrio&apos;s verified contact details are configured.</div>}</div><LeadForm source="CONTACT" title="Tell us what is on your mind" /></div></Container></Section></> }
