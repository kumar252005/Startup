import Link from "next/link";
import { ArrowRight, BarChart3, CircleUserRound, Handshake, Inbox } from "lucide-react";
import { getLeadMetrics } from "@/lib/db";

const cards = [["Total leads", "total", Inbox], ["New leads", "newLeads", CircleUserRound], ["Consultations", "consultations", BarChart3], ["Projects won", "won", Handshake]] as const;
export default async function AdminPage() { let metrics = { total: 0, newLeads: 0, consultations: 0, won: 0, conversionRate: 0 }; let configured = true; try { metrics = await getLeadMetrics(); } catch { configured = false; } return <><div className="flex flex-col gap-2 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-medium text-zinc-500">Lead management</p><h1 className="mt-1 text-3xl font-semibold tracking-tight text-zinc-950">Overview</h1></div><Link href="/admin/leads" className="inline-flex items-center gap-2 text-sm font-semibold text-blue-600 hover:text-blue-700">View all leads <ArrowRight className="size-4" /></Link></div>{!configured && <div className="mt-7 rounded-md border border-amber-200 bg-amber-50 p-4 text-sm text-amber-800">Database access is not configured for this deployment. Add `DATABASE_URL`, run the migration, then refresh this dashboard.</div>}<div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label, key, Icon]) => <div className="rounded-lg border border-zinc-200 bg-white p-5" key={label}><div className="flex items-center justify-between"><p className="text-sm text-zinc-500">{label}</p><Icon className="size-4 text-zinc-400" /></div><p className="mt-5 text-3xl font-semibold tracking-tight text-zinc-950">{metrics[key]}</p></div>)}<div className="rounded-lg border border-blue-200 bg-blue-50 p-5"><p className="text-sm text-blue-700">Conversion rate</p><p className="mt-5 text-3xl font-semibold tracking-tight text-blue-950">{metrics.conversionRate}%</p></div></div></>; }
